# Optional stack for OTLP export (profile: otel). UI: http://localhost:16686
services:
  jaeger:
    image: jaegertracing/all-in-one:1.76
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"
      - "4318:4318"
