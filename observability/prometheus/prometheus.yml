global:
  scrape_interval: 10s

scrape_configs:
  - job_name: demo
    metrics_path: /actuator/prometheus
    static_configs:
      # Windows/Mac Docker Desktop: reach host machine from containers
      - targets: ["host.docker.internal:8080"]
